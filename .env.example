# Passcode for the Bid Command Center login screen.
# If unset, the app falls back to "elite" so a fresh clone runs immediately.
BCC_PASSCODE=change-me

# Secret used to sign the session cookie. Any long random string.
BCC_SESSION_SECRET=change-me-too
